// 配列と新しい+>、?>パイプ演算子のテスト

// 数値を2倍にする関数
def double() : int -> int {
    🍕 * 2 >> 💩
}

// 偶数かどうかを判定する関数
def is_even() : int -> bool {
    🍕 % 2 == 0 >> 💩
}

// テスト用の配列を作成 (範囲を使用)
[1..5] >> numbers;

// map演算子を使って各要素を2倍にする
"mapで2倍にした結果:" |> print;
numbers +> double >> doubled;
doubled |> print;

// filter演算子を使って偶数のみを取り出す
"filterで偶数のみを取り出した結果:" |> print;
is_even(2); // 関数を一度実行しておく
numbers ?> is_even >> evens;
evens |> print;

// 元の配列は変更されていないことを確認
"元の配列(変更されていないことを確認):" |> print;
numbers |> print;
