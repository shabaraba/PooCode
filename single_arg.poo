// 関数引数のシンプルなテスト

// 挨拶関数の定義
def greet(name) {
    "Hello, " + name + "\\!" >> 💩
}

// 通常の関数呼び出し
greet("Claude") |> print;

// 数値計算関数
def multiply(factor) {
    🍕 * factor >> 💩
}

// パイプラインでの関数呼び出し
5 |> multiply(3) |> print;  // 15が表示されるはず

// 引数の検証をするために明示的に複数のステップに分ける
10 >> num;
num |> multiply(4) >> result;
result |> print;          // 40が表示されるはず

// 同じ関数をさまざまな値でテスト
2 |> multiply(6) |> print;  // 12が表示されるはず
