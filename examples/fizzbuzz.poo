def is_multiple_of(num): int -> bool {
    🍕 % num == 0 >> 💩;
}

def showFizz() if 🍕 % 3 == 0 : int -> str {
    "Fizz" >> 💩;
}
def showFizz() : int -> str{
    "" >> 💩;
}
 
def showBuzz() if 🍕 % 5 == 0 : int -> str {
    "Buzz" >> 💩;
}
def showBuzz() : int -> str {
    "" >> 💩;
}

def showNumber(num) if 🍕 == "" : str -> str {
    num |> to_string >> 💩;
}
def showNumber(num) : str -> str {
    🍕 >> 💩;
}
 
def fizzbuzz() : int -> str {
    🍕 |> print;
    "" >> result;
    
    🍕 |> showBuzz |> add result >> result;
    🍕 |> showFizz |> add result |> showNumber 🍕 >> 💩;
}

//1 |> fizzbuzz |> print;
[1..20] |> map fizzbuzz |> print;
