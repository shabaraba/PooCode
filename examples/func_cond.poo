// 修正版: 条件分岐による関数オーバーロードのテスト
def test(num) if 🍕 == 1 : int -> int {
    // 🍕が1の場合はこの関数が呼ばれる
    // 数値1に🍕を加算して返す
    1 + 🍕 >> 💩
}
def test(num): int -> int {
    // それ以外の場合はこの関数が呼ばれる
    // そのまま値を返す
    🍕 >> 💩 
}

// テスト: 値を表示して確認する
"テスト1: " |> print
0 |> test 1 |> print  // 🍕は0なので下のtestが呼ばれて0が返るはず 

"テスト2: " |> print
1 |> test 1 |> print  // 🍕は1なので上のtestが呼ばれて2(=1+1)が返るはず
