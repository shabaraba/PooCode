// 事前関数登録の改善によるコメントアウト問題の検証テスト
// このテストは、関数呼び出しがコメントアウトされていても
// マップ演算子（+>）で関数が正しく使用できることを確認します

def showTest() : int -> str {
    🍕 |> print;
    "" >> result;
    
    "Result from showTest: " + (🍕 |> to_string) >> 💩;
}

// 通常の場合、最初の関数呼び出しをコメントアウトすると
// 以前はこの行でshowTest関数が環境に登録されていなかった
// 5 |> showTest |> print;

// 今回の修正によって、関数が事前に環境に登録されるため
// この関数呼び出しも問題なく動作するはず
[1..3] +> showTest +> print;
