// テスト: 条件付き関数の動作確認
def test(num) if 🍕 == 1 : int -> int {
    "🍕 is 1, num is " + num >> 💩
}

def test(num): int -> int {
    "🍕 is not 1, 🍕 = " + 🍕 + ", num = " + num >> 💩
}

// テスト: 条件に一致する場合
"テスト1:" |> print
1 |> test(2) |> print  // 🍕は1なので上のtestが呼ばれるはず

// テスト: 条件に一致しない場合
"テスト2:" |> print
0 |> test(2) |> print  // 🍕は0なので下のtestが呼ばれるはず
