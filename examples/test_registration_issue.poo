// 最小限のテストケース

// ケース1: 通常の定義と呼び出し順序（関数が最初に定義され、その後呼び出される）
def function1() : int -> str {
    "Function 1 called with: " + (🍕 |> to_string) >> 💩;
}

100 |> function1 |> print;

// ケース2: 関数呼び出しがコメントアウトされた定義と呼び出し
def function2() : int -> str {
    "Function 2 called with: " + (🍕 |> to_string) >> 💩;
}

// この行がコメントアウトされている
// 200 |> function2 |> print;

// あとから呼び出す
300 |> function2 |> print;