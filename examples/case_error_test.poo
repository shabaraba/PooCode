// case_error_test.poo - case文のエラーケースのテスト

// エラーケース1: 関数外でのcase文使用（エラーになるはず）
"関数外でのcase文使用テスト:" |> print
case 1 == 1:
  "これは実行されません" >> 💩

// 正しい使い方の例
"関数内でのcase文使用テスト:" |> print

func test() {
  case 🍕 > 10:
    "10より大きい" >> 💩
  case default:
    "10以下" >> 💩
}

5 |> test |> print  // "10以下"を表示
15 |> test |> print // "10より大きい"を表示

// エラーケース2: caseの後に条件式がない
"条件式なしのcase文テスト:" |> print
func test_invalid() {
  case:
    "条件式なし" >> 💩
}

// エラーケース3: caseの後にブロックがない
"ブロックなしのcase文テスト:" |> print
func test_no_block() {
  case 🍕 > 0
}
