// 条件付き関数のデバッグテスト - 最終バージョン
// 条件: 🍕 == 1 の場合のみ実行される関数

// ===================== 関数定義 =====================
// 条件式タイプ1: 🍕 == 1 の場合のみ一致する関数
def test_func() if 🍕 == 1 : int -> string {
    "条件一致: 🍕 = " + to_string(🍕) >> 💩
}

// デフォルト関数: 上記の条件に一致しない場合に実行
def test_func(): int -> string {
    "デフォルト: 🍕 = " + to_string(🍕) >> 💩
}

// ===================== テスト実行 =====================
"===== 条件付き関数テスト開始 =====" |> print;

"テスト1: 入力 = 0 (条件不一致なのでデフォルト関数が実行されるはず)" |> print;
0 |> test_func |> print;

"テスト2: 入力 = 1 (条件一致なので条件付き関数が実行されるはず)" |> print;
1 |> test_func |> print;

"テスト3: 入力 = 2 (条件不一致なのでデフォルト関数が実行されるはず)" |> print;
2 |> test_func |> print;
