// 単純な条件付き関数テスト - numを使わないバージョン
def simple_test() if 🍕 == 1: int -> int {
    // 🍕が1の場合は10倍して返す
    🍕 * 10 >> 💩
}

def simple_test(): int -> int {
    // それ以外は自身を返す
    🍕 >> 💩
}

// テスト
"1の場合:" |> print
1 |> simple_test |> print  // 10が返るはず
"2の場合:" |> print
2 |> simple_test |> print  // 2が返るはず
