// 配列と新しいmap演算子(+>)のテスト

// 数値を2倍にする関数
def double() : int -> int {
    🍕 * 2 >> 💩
}

// テスト用の配列を作成 (範囲を使用)
[1..5] >> numbers;

// +>演算子を使って各要素を2倍にする
"元の配列:" |> print;
numbers |> print;

"mapで2倍にした結果(+>演算子):" |> print;
numbers +> double >> doubled;
doubled |> print;

"元の配列(変更されていないことを確認):" |> print;
numbers |> print;
