// 条件式評価の簡単なテスト - スペース演算子バージョン
// 🍕変数の参照と型の扱いを確認

// 条件式付き関数 - 🍕 == 1 の場合のみ実行
def test_space() if 🍕 == 1 : int -> string {
    "パターン1: " + "🍕 = 1"
}

// デフォルト関数 - 上記の条件が偽の場合に実行
def test_space(): int -> string {
    "パターン2: " + "🍕 != 1"
}

// 型のテスト
"テスト結果:" |> print;
0 |> test_space |> print;
1 |> test_space |> print; 
2 |> test_space |> print;
