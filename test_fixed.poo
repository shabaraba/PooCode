// 条件付き関数のシンプルなテスト

// 条件付き関数の定義
def test_cond(num) if 🍕 == 1 : int -> int {
    // 🍕が1の場合は加算して返す
    🍕 |> add 10 >> 💩
}

def test_cond(num): int -> int {
    // それ以外は2倍して返す
    🍕 |> add 🍕 >> 💩
}

// テスト1: 🍕 = 0の場合
"テスト1 (🍕=0): " |> print
0 |> test_cond 1 |> print  // 期待値: 0 (0+0=0)

// テスト2: 🍕 = 1の場合
"テスト2 (🍕=1): " |> print
1 |> test_cond 1 |> print  // 期待値: 11 (1+10=11)

// テスト3: 🍕 = 2の場合
"テスト2 (🍕=2): " |> print
2 |> test_cond 1 |> print  // 期待値: 4 (2+2=4)
