// デバッグ用: 条件式のテスト
def test(num) if 🍕 == 1 : int -> int {
    🍕 + num >> 💩;
}

def test(num): int -> int {
    🍕 >> 💩;
}

// テスト
// "1" |> test(10) |> print;  // 🍕は1
1 |> test(20) |> print;  // 🍕は0
