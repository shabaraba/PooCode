// 条件式評価の簡単なテスト
// 🍕変数の参照と型の扱いを確認

// 条件式付き関数 - 🍕 == 1 の場合のみ実行
def test_cond() if 🍕 == 1 : int -> string {
    "条件式が真になりました: 🍕=" + to_string(🍕)
}

// デフォルト関数 - 上記の条件が偽の場合に実行
def test_cond(): int -> string {
    "デフォルト関数を実行: 🍕=" + to_string(🍕)
}

// 型のテスト
"テスト結果:" |> print;
0 |> test_cond |> print;
1 |> test_cond |> print;
2 |> test_cond |> print;
